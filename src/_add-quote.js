// Add a quote to the Mongodb collection
const form = document.getElementsById('form__quote-add')

// Get all field values into an object

// Validate each field

// Submit object via axios

// Submit object via quote.js
